export enum Routes {
  DASHBOARD = '/',
  SEARCH = '/v2',
  MYDIAMONDS = '/',
  SAVEDSEARCH = '/',
  APPOINTMENTS = '/',
  MYCART = '/',
  SETTINGS = '/'
}
// Enum using the enum keyword
export enum SubRoutes {
  NEW_SEARCH = 'new-search',
  RESULT = 'result'
}
